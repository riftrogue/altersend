import { rawTokens } from './tokens.raw'

export const space = rawTokens.space
export const radius = rawTokens.radius
export const fontSize = rawTokens.fontSize
export const fontWeight = rawTokens.fontWeight
export const lineHeight = rawTokens.lineHeight
export const fontFamily = rawTokens.fontFamily
